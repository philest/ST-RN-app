import React from 'react';
import {
  View,
  Text
} from 'react-native'
import { connect } from 'react-redux'

export default ReaderStandin = ({}) => (
  <View style={{flex:1}}>
    <Text>Pee</Text>
  </View>
)
