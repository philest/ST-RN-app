import React from 'react';
import {
  View,
  Text
} from 'react-native'
import { connect } from 'react-redux'

export default SplashStandin = ({}) => (
  <View style={{flex:1}}>
    <Text>Poop</Text>
  </View>
)
